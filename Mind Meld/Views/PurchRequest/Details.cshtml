@model Mind_Meld.Models.PurchRequest

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_PurchEmpLayout.cshtml";
}

<!--This is a header of our page you can copy and past it in any new view u create, Lets Goo!-->
<div class="content container-fluid">

    <div class="page-header">
        <div class="row">
            <div class="col-md-6">
                <h3 class="page-title mb-0">Manage Purchase Request</h3>
            </div>
            <div class="col-md-6">
                <ul class="breadcrumb mb-0 p-0 float-right">
                    <li class="breadcrumb-item"> <a asp-controller="Dashbroad" asp-action="DeptEmpDashboard"><i class="fas fa-home"></i> Home</a></li>
                    <li class="breadcrumb-item"> <a asp-controller="PurchRequest" asp-action="Index"><i></i> Details Purchase Request</a></li>
                    <li class="breadcrumb-item"> <a asp-controller="PurchRequest" asp-action="Create"><i></i> Add Purchase Request</a></li>
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="content container-fluid">
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.RequestDate)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.RequestDate)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.User)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.User.Id)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PRStatus)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PRStatus)
        </dd>
    </dl>
</div>

<div>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.RequestID">Edit</a>
    |
    <a class="btn btn-secondary" asp-action="Index">Back to List</a>
</div>
